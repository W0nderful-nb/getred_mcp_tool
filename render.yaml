services:
  - type: web
    name: getred-mcp
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install chromium"
    startCommand: "python app.py"
    plan: free
    envVars: []
